cmake_minimum_required(VERSION 3.16.0)

# Include the ESP-IDF core CMake build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define the master project name
project(tuya_32ch_smart_relay)